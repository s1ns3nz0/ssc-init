{
  "mcpServers": {
    "filesystem": {
      "command": "$HOME/.local/bin/filesystem-mcp",
      "args": [
        "--mode",
        "fixture",
        "--root=$HOME/Projects"
      ],
      "env": {
        "GITHUB_TOKEN": "fixture-secret"
      }
    }
  }
}
