# SDD ledger — plan: docs/superpowers/plans/2026-08-08-program-b-status-retention-budgets.md

Program B of the full-design roadmap (gap inventory: 142 capabilities, 33 BUILT / 24 PARTIAL / 85 MISSING).
Authority: docs/superpowers/specs/2026-08-05-ssc-init-design.md sections 10, 11, 12.
Branch base: 39e444e, working on master.
Why first: unbounded growth is a LIVE DEFECT. Measured on the reference machine after one day: state.db 87 MB, 36 snapshots, 11246 assets, 16184 observations, 32609 evidence rows. Only content_cache prunes (internal/store/content_cache.go:96-110); scans/assets/observations/evidence never do. ssc-init hook persists a full snapshot per session and is wired to SessionStart, so the section 12 500 MB cap is already fiction.
Correction to the gap inventory: it reported CLAUDE.md "Active work" as stale. It is not — that section was rewritten earlier; the only remaining "worktree" mentions are build-script wording.
Task 1: dispatched (base 39e444e)
Task 1 implementation: 9dd0d3b; RED (undefined ScanStatus), GREEN 7 packages + race + vet + gofmt + diff. Wire format proven unchanged — no golden edited; repo has no .golden files, contracts are inline expected-JSON in report/model/acceptance tests, all passed unmodified. Adaptation: scan_test.go is package model (not model_test), so the plan's model.* qualifiers were dropped. Compile fixes in 4 files: report/json.go (payload retyped, no boundary cast), report/pretty.go (human-render cast only), store/validation.go (cast inside the existing required-string gate; Task 2 owns Valid()), scan/service.go (overallStatus return type). internal/cli needed none — statusPayload carries no status field. database/sql round-trip verified by passing store+acceptance suites, not inspection.
Task 1 review: dispatched (base bf34c91, head 9dd0d3b)
Task 2: dispatched (base 9dd0d3b)
Task 2 implementation: e0b3c1f; RED both cases (save accepted "ok"; load accepted after UPDATE scans SET status='ok'), GREEN store/acceptance/scan + race + 20x. Reused real helpers openTestStore/assertNoSnapshotRows (snapshots_test.go) and validV3Snapshot/saveValidV3Snapshot (evidence_test.go) — the plan's placeholder helper names do not exist. Both checks kept: required-string proves presence AND runs validateOptionalString (UTF-8 + secret scan), Valid() proves vocabulary.
Task 2 review: dispatched (base 9dd0d3b, head e0b3c1f)
Task 3: dispatched (base e0b3c1f) — the live-defect fix
Task 1 complete: 9dd0d3b; review CLEAN. Byte-comparison proof: binaries built at both commits, run against separate identical copies of testdata/home — scan --baseline --json differed ONLY in scanId + the two timestamps (single diff hunk, lines 1-2, nothing else in 63KB moved); status --json RAW byte-identical, no normalization. Stronger: testdata/golden/baseline.json IS a fixed-clock byte-exact golden (bytes.Equal at acceptance/content_evidence_test.go:350), untouched by the diff and passing at BOTH commits. DB round-trip proven by throwaway test — all four values reload equal with typeof(status)='text'. 3/3 mutations killed. stale/blocked confirmed genuinely unreachable (only overallStatus writes Status, returning partial|complete).
  Reviewer corrections to the implementer's report: the repo DOES have a golden (testdata/golden/baseline.json) — "no .golden files" was literally true but incomplete; rung_test.go has NO inline expected JSON (misnamed); and internal/cli/run_test.go:231 is a real inline expected-JSON site the implementer did not name. All untouched.
  COVERAGE GAP for later tasks: mutating ScanComplete to "Complete" was caught by exactly ONE test (internal/scan). report/store/acceptance/cli all passed, because their fixtures assign the untyped literal "complete" rather than the constant. The golden home yields partial, so no golden pins the complete wire literal.
  Minor asymmetry: ScanStale's comment states it is unreachable; ScanBlocked's does not.
Task 3 implementation: c85a66f (LIVE DEFECT FIX). Cascade determination: NO cascade — every FK in migrations.go is NO ACTION, pinned by requiredForeignKeys and asserted by verifyForeignKeys at every Open. Pragma IS enforced: _pragma=foreign_keys(1) is in the DSN (sqlite.go:144-152) so it applies to every connection, verifyPragmas asserts it, and SetMaxOpenConns(1). Behavioural proof: parent-only DELETE failed with FOREIGN KEY constraint failed (787) — so the naive implementation would have ABORTED the SaveScan transaction, not silently orphaned. pruneSnapshots deletes 12 scan_id-keyed child tables in reference order then scans, inside the existing transaction. No migration needed.
  Newest-snapshot rule: implemented by resolving the newest id ONCE before any delete (same finished_at DESC, id DESC ordering LatestSnapshot uses) and excluding it by bound parameter — deliberately not a correlated subquery, whose re-evaluation during deletion is unspecified. Tested with a single 365-day-old snapshot surviving.
  Orphan test enumerates child tables from the LIVE schema (sqlite_master JOIN pragma_table_info WHERE c.name='scan_id', asserting >=12) rather than a hardcoded list, so a future table cannot escape it.
  Measurement (40 snapshots x ~300 assets, spanning 45 days): scans 40->27, assets 12000->8100, observations 24000->16200, evidence 48000->32400 (-32.5%), zero orphans. File did NOT shrink: 83.02 MB with 7223 freelist pages (~28 MB) and WAL grown to 19.39 MB. Growth is now BOUNDED (plateaus at ~30 days, reuses freed pages) but filesystem reclamation is still owed — Task 4.
Task 3 review: dispatched (base 8b12f02, head c85a66f)
Task 4: dispatched (base c85a66f) — space reclamation
Task 2 complete: e0b3c1f; review CLEAN. Guard placement verified total: validateSnapshot has exactly two production callers (SaveScan before any SQL; LatestSnapshot after rows load), LatestInventory inherits it, and there is exactly ONE non-test write to scans and ONE non-test read — both gated. 3/3 mutations killed, including one proving the load subtest is load-specific rather than incidental. Load test proven non-tautological: 'ok' is non-empty, valid UTF-8 and secret-free, so nothing but Valid() can reject it.
  LEDGER CORRECTION (the Task 2 entry above is wrong): the implementer's stated rationale for keeping BOTH checks does not hold. Valid() admits exactly four ASCII literals, so no value surviving it can be empty, invalid UTF-8, or secret-shaped — the UTF-8 and secret scans are unreachable on this field. Reviewer empirically deleted the required-string entry and the whole suite still passed. It is harmless redundancy (three lines of dead weight in a belt-and-braces validator), NOT the meaningful second gate the implementer claimed. Residual difference is error selection only: the map loop runs first, so a credential-shaped status returns ErrSensitiveSnapshot instead of the generic message; nothing matches on that sentinel for this field. Legitimate cleanup for a later task; keeping it blocks nothing.
  Compatibility verified: the load guard makes an out-of-vocabulary persisted status a hard read failure with no fallback, but every historical revision of overallStatus returned only complete|partial, so no shipped state.db can be bricked. Hard-fail matches the package's existing behaviour for corrupt scope_json.
  Informational, out of scope: internal/store/sqlite.go:247-290 interpolates the absolute database path into eight error messages. That is the store's own state.db path, never persisted snapshot content.
Task 4 implementation: 8e6541c; RED (9651136 bytes after pruning vs 6738312 peak), GREEN + race + 20x. reclaimPrunedSpace runs POST-commit (data already durable), gated on freelist >= 256 pages AND >= 25% of file, then VACUUM + wal_checkpoint(TRUNCATE). ~30 lines, no migration, no pragma change.
  KEY FINDING: in WAL mode VACUUM writes the rewritten db THROUGH the log — measured 48.87 MB db + 49.16 MB WAL immediately after vacuuming, i.e. footprint gets WORSE until checkpointed. Checkpoint costs 0.5-0.7ms. The test measures state.db + -wal together, so a vacuum that merely relocated waste into the log fails it.
  auto_vacuum REJECTED with measurements, not opinion: converting an existing store costs 236ms + a 74MB WAL blow-up (the exact full rewrite it exists to avoid, on the reference machine's shipped 87MB store) to save 90ms per rare reclamation; plus permanent pointer-map overhead, a non-transactional special case in applyMigrations, and reordered pragma verification. verifyPragmas therefore untouched.
  Reclamation errors swallowed deliberately: data is committed, so returning an error would report a successful save as failed and invite a retry colliding on the duplicate scan ID. The freelist survives, so the next save retries.
  Measurement (same 40-snapshot/45-day shape as Task 3): rows 96800->65340 (-32.5%, same as Task 3), page_count 18209->11932, freelist 6164->0, footprint 92.53 MB -> 46.61 MB (-50%).
  Latency vs the ~1s SessionStart budget: +143ms on the ONE save that triggers it (~200ms extrapolated to an 87MB store), 0ms on every other save (two sub-ms pragma reads). Steady state measured 432 free pages of 12379 (3.5%) — gate held, save cost 264ms, no rewrite.
  Deliberately skipped: a test pinning the gate thresholds — an inverted gate shows up as latency, not incorrectness, and would still pass the reclaim test. Add one if thresholds become configurable.
Task 4 review: dispatched (base c85a66f, head 8e6541c)
Task 5: dispatched (base 8e6541c) — asset change history retention
Task 5 implementation: 2079888. Design determination CONFIRMED: no history existed — every table is scan_id-keyed with an FK to scans(id) (only content_cache is not, and it is a digest cache), so before this commit section 10's 90-day history was unsatisfiable. Migration 6 adds asset_history: one row per distinct asset, NO scan_id and NO foreign key by design; the schema test rejects an added scan_id FK, which would reintroduce the coupling the table exists to avoid. Columns each justified against a question: asset_id, first_seen_at (the section 10 question a pruned snapshot can no longer answer), last_seen_at (also the 90-day clock), content_digest (sha256 over complete evidence ID\0digest pairs — only complete is trusted, so CHANGED survives without retaining evidence rows), content_changed_at. Rejected: version column (already in the ID), per-evidence rows (3x rows, and evidence-free assets would have no first-seen).
  Two commented rules: an empty digest never overwrites a known one (an unreadable target must not forge a change record); history is never pruned for an asset a retained snapshot still names.
  Privacy rides the EXISTING validateSnapshot gate — every value is a validated asset ID or a digest of validated digests, no parallel path. asset_history added to assertNoSnapshotRows so ~15 existing rejection tests now cover it. Digests cannot carry a secret (validateEvidenceShape constrains complete evidence to lowercase sha256 hex).
  Migration backfills first/last seen from min/max(scans.finished_at), so the reference machine's 36 snapshots contribute history instead of losing it at the next prune. Repo has no down-migration mechanism; migration 6 is purely additive and transactional.
  Storage (40 saves/45 days, 500 assets, 5% churn): 1477 rows, 184 KB, ~2.1% of an 8.2 MB store. O(distinct assets ever seen), flat in snapshot count. Extrapolated to the reference machine: ~1.9 MB against 87 MB, vs the ~3x growth retaining whole snapshots for 90 days would have cost.
  Fixed a version-pinned pre-existing test (TestMigration5... asserted applied != 5); now uses len(migrations) so migration 7 will not break it.
Task 5 review: dispatched (base 8e6541c, head 2079888)
Task 6: dispatched — configurable retention windows
Task 3 fix round 1: content complete in HEAD, attribution split across 2079888 and 97384fc.
  CONTROLLER ERROR (recorded so it is not repeated): two writers were dispatched into internal/store in the SAME worktree. The Task 5 agent ran `git add internal/store/{migrations,snapshots,snapshots_test}.go` while the Task 3 fix agent's edits were unstaged, sweeping F1/F2/F3's tests and the F2 comment into 2079888. Nothing was clobbered (Task 5's hunks verified intact) and the fix agent correctly declined to rewrite someone else's commit. DISCIPLINE CHANGE: serialize same-package writers, or require each to commit before the next is dispatched.
  F1 TestSaveScanKeepsSnapshotsInsideRetentionWindow — zero-window mutant now fails (kept 1, want 4). Note the reviewer's "passes everything" claim predated 8e6541c; Task 4's reclaim test also catches it now.
  F2 TestSaveScanKeepsSnapshotExactlyAtRetentionEdge + an inclusive-window comment on snapshotRetention. The `<=` mutant fails only that test.
  F3 solved better than either option offered: NO failure seam (would ship test-only machinery into a release-blocking write path) and NO bare comment (kills nothing). The test installs a SQLite trigger (BEFORE DELETE ON scans -> raise(ABORT)) forcing the prune's final delete to fail from OUTSIDE the process. In-transaction prune -> save errors, baseline absent; post-commit prune -> baseline already durable, test fails. Zero production change, asserts the invariant rather than the call ordering.
  F4 per-table drop matrix, all 12 now killed. Previously SURVIVED with zero failures: relationship_state, relationships, coverage, inventory_errors — the fixture wrote no rows for them. validV3Snapshot now emits one coverage result, one relationship, one inventory error. asset_history correctly does not enter scanChildTables (no scan_id column).
Task 6: dispatched — configurable retention windows (sole writer in internal/store)
Task 6 implementation: 441e3f9. Options{SnapshotRetention, AssetHistoryRetention} + OpenWithOptions; Open(path) is now OpenWithOptions(path, Options{}) with an unchanged signature, so no caller changed. No config file, env var, or CLI flag — in-process seam only, as scoped (Program C owns policy-driven config).
  ZERO-VALUE TRAP CONFIRMED BY BEHAVIOUR: the intermediate naive implementation used option windows verbatim and TestOpenZeroOptionsRetainsTheDocumentedDefaults failed with "zero options kept 1 snapshots, want 2 (29 days is inside the 30 day default)", dragging down 5 further tests. Resolution is window <= 0 -> default, so a NEGATIVE window (cutoff in the future, which would prune unaged snapshots) is caught too.
  Deliberately skipped: exported default constants and a retention accessor on Store — Task 8 adds whichever shape doctor needs.
Task 6 review: dispatched (base b7a255b, head 441e3f9)
Task 8: dispatched — doctor reports store size and retention (sole writer in internal/store + internal/doctor)
Task 4 complete: 8e6541c; review CLEAN, no product change required. Verification was empirical, not inspectional: 15 SIGKILLs mid-VACUUM (13 landing genuinely inside it) — all 15 reopened with integrity_check=ok, zero FK violations, committed snapshot present, subsequent save succeeded. Context cancellation at 0/2/5/8/30ms all safe. Two Stores saving concurrently on one file: both nil, both persisted.
  secureSQLiteFiles interaction PASS: VACUUM PRESERVES THE INODE (22854956 before and after), so the retained guard fd and os.SameFile check stay valid — this is the mechanism that would have broken had SQLite rewritten via rename. Raw modes measured under umask(0) immediately after VACUUM and BEFORE any chmod: db/wal/shm all 0600, so the subsequent chmod is belt-and-braces, not load-bearing. Zero temp-file residue (vacuum temp db is DELETEONCLOSE; watched store dir + redirected TMPDIR at 100us, plus after four mid-vacuum SIGKILLs).
  FOUR CORRECTIONS to Task 4's report and this ledger:
  (1) Magnitude/shape wrong: reduction is 97% (57.07 MB -> 1.90 MB), not 50%, and it only happens when a batch ages out at once. In the cited 40-snapshot/45-day STEADY STATE the gate NEVER OPENS (3.6% free vs 25% threshold), so 92.53 -> 46.61 MB is not reproducible for that shape. Task 3's "7223 freelist pages" is ~17x what the reviewer measures for the same stated shape.
  (2) Latency understated: triggering save is +334ms on a 50MB store and +612ms on the 73MB reference shape, not +143ms. Gate-closed cost confirmed at 3.6us/call.
  (3) The error-swallowing rationale is FACTUALLY WRONG: scan IDs are random UUIDv4 (scan/service.go:122), so a retry cannot collide on a duplicate ID, and there is no automatic retry. The decision is still correct, for a stronger reason: on a save error Scan discards the whole ScanResult/Inventory/Delta, so the user would lose the report for data that IS persisted.
  (4) Mutation (b) — fully inverted gate — IS killed by TestSaveScanRetentionReclaimsFileSpace. The implementer was too pessimistic. What actually survives is LOOSENING (gate removed entirely, or thresholds set to fire always): accepted, since that shows up as latency not incorrectness.
  New findings, all LOW/INFO: a concurrent reader makes wal_checkpoint(TRUNCATE) wait out the 5s busy_timeout and fail, leaving old-size db + large WAL and adding 5.25s to the save (self-heals at Close); reclamation NEVER fires under daily-cadence scanning (freelist pins at 3.6%, file plateaus while still creeping); a store that can never reclaim is completely silent — reviewer recommends doctor report freelist_count*page_size, which has been relayed to the in-flight Task 8.
  REFERENCE MACHINE (read-only inspection of a copy): 40 scans, page_count 24578 = 100.67 MB, freelist 0, all timestamps within one day. Reclamation will NOT fire on the next scan; the store keeps growing for 30 days. It fires on the first scan on or after 2026-09-07 when the single-day batch ages out at once — modelled 79.34 MB -> 3.74 MB at a cost of 768ms on that one save. If instead scanning continues daily, the gate never opens and the store plateaus in the 100 MB range.
Task 7 implementation: 0c9a373. Budget field + DefaultBudget (10m per section 12 baseline); budgetCtx = WithTimeout(ctx, Budget) passed to orchestrator, project-MCP follow-up, and evidence engine, while the CALLER's ctx remains what every post-phase check and all persistence uses.
  FOUND A REAL PRE-EXISTING DEFECT: the cancellation path was NOT clearing sealed runtime state — the early return on ctx.Err() dropped results without clearing, violating the release-blocking "cancellation propagates AND clears partial runtime state" invariant. Caught by the new test (cancelled scan kept runtime state: issuer=false provenance=false), fixed with clearRuntimeState. internal/scan had no prior scan-level cancellation test at all.
  Design: parent-context check, NOT context.Cause. The caller's ctx is done only if the caller cancelled or their own deadline expired, so "ctx.Err()==nil while collection came back short" is unambiguously a budget overrun. context.Cause would need WithTimeoutCause plus care that Cause(budgetCtx) returns the PARENT's cause when the parent cancels first — exactly the conflation to avoid. A caller deadline shorter than the budget trips the caller-ctx check first and is treated as cancellation (error, nothing persisted).
  Naming unscanned collectors needed no new mechanism: the orchestrator's existing collector_timeout conversion fires for anything still running when budgetCtx expires, landing them in Coverage as failed; overallStatus already returns ScanPartial; the evidence engine yields terminal unavailable/time_limit.
  No existing test modified or deleted — additive except the Budget field, budgetCtx plumbing, and clearRuntimeState.
  FOLLOW-UP (not done): nothing in internal/cli or cmd/ssc-init sets Budget, so both get the 10m default. Section 12 wants <=60s for the daily incremental path; that wiring is a separate task.
Task 7 review: dispatched (head 0c9a373)
Task 9: dispatched — performance budget harness
