# SDD ledger — plan: docs/superpowers/plans/2026-08-08-program-b-status-retention-budgets.md

Program B of the full-design roadmap (gap inventory: 142 capabilities, 33 BUILT / 24 PARTIAL / 85 MISSING).
Authority: docs/superpowers/specs/2026-08-05-ssc-init-design.md sections 10, 11, 12.
Branch base: 39e444e, working on master.
Why first: unbounded growth is a LIVE DEFECT. Measured on the reference machine after one day: state.db 87 MB, 36 snapshots, 11246 assets, 16184 observations, 32609 evidence rows. Only content_cache prunes (internal/store/content_cache.go:96-110); scans/assets/observations/evidence never do. ssc-init hook persists a full snapshot per session and is wired to SessionStart, so the section 12 500 MB cap is already fiction.
Correction to the gap inventory: it reported CLAUDE.md "Active work" as stale. It is not — that section was rewritten earlier; the only remaining "worktree" mentions are build-script wording.
Task 1: dispatched (base 39e444e)
Task 1 implementation: 9dd0d3b; RED (undefined ScanStatus), GREEN 7 packages + race + vet + gofmt + diff. Wire format proven unchanged — no golden edited; repo has no .golden files, contracts are inline expected-JSON in report/model/acceptance tests, all passed unmodified. Adaptation: scan_test.go is package model (not model_test), so the plan's model.* qualifiers were dropped. Compile fixes in 4 files: report/json.go (payload retyped, no boundary cast), report/pretty.go (human-render cast only), store/validation.go (cast inside the existing required-string gate; Task 2 owns Valid()), scan/service.go (overallStatus return type). internal/cli needed none — statusPayload carries no status field. database/sql round-trip verified by passing store+acceptance suites, not inspection.
Task 1 review: dispatched (base bf34c91, head 9dd0d3b)
Task 2: dispatched (base 9dd0d3b)
Task 2 implementation: e0b3c1f; RED both cases (save accepted "ok"; load accepted after UPDATE scans SET status='ok'), GREEN store/acceptance/scan + race + 20x. Reused real helpers openTestStore/assertNoSnapshotRows (snapshots_test.go) and validV3Snapshot/saveValidV3Snapshot (evidence_test.go) — the plan's placeholder helper names do not exist. Both checks kept: required-string proves presence AND runs validateOptionalString (UTF-8 + secret scan), Valid() proves vocabulary.
Task 2 review: dispatched (base 9dd0d3b, head e0b3c1f)
Task 3: dispatched (base e0b3c1f) — the live-defect fix
Task 1 complete: 9dd0d3b; review CLEAN. Byte-comparison proof: binaries built at both commits, run against separate identical copies of testdata/home — scan --baseline --json differed ONLY in scanId + the two timestamps (single diff hunk, lines 1-2, nothing else in 63KB moved); status --json RAW byte-identical, no normalization. Stronger: testdata/golden/baseline.json IS a fixed-clock byte-exact golden (bytes.Equal at acceptance/content_evidence_test.go:350), untouched by the diff and passing at BOTH commits. DB round-trip proven by throwaway test — all four values reload equal with typeof(status)='text'. 3/3 mutations killed. stale/blocked confirmed genuinely unreachable (only overallStatus writes Status, returning partial|complete).
  Reviewer corrections to the implementer's report: the repo DOES have a golden (testdata/golden/baseline.json) — "no .golden files" was literally true but incomplete; rung_test.go has NO inline expected JSON (misnamed); and internal/cli/run_test.go:231 is a real inline expected-JSON site the implementer did not name. All untouched.
  COVERAGE GAP for later tasks: mutating ScanComplete to "Complete" was caught by exactly ONE test (internal/scan). report/store/acceptance/cli all passed, because their fixtures assign the untyped literal "complete" rather than the constant. The golden home yields partial, so no golden pins the complete wire literal.
  Minor asymmetry: ScanStale's comment states it is unreachable; ScanBlocked's does not.
Task 3 implementation: c85a66f (LIVE DEFECT FIX). Cascade determination: NO cascade — every FK in migrations.go is NO ACTION, pinned by requiredForeignKeys and asserted by verifyForeignKeys at every Open. Pragma IS enforced: _pragma=foreign_keys(1) is in the DSN (sqlite.go:144-152) so it applies to every connection, verifyPragmas asserts it, and SetMaxOpenConns(1). Behavioural proof: parent-only DELETE failed with FOREIGN KEY constraint failed (787) — so the naive implementation would have ABORTED the SaveScan transaction, not silently orphaned. pruneSnapshots deletes 12 scan_id-keyed child tables in reference order then scans, inside the existing transaction. No migration needed.
  Newest-snapshot rule: implemented by resolving the newest id ONCE before any delete (same finished_at DESC, id DESC ordering LatestSnapshot uses) and excluding it by bound parameter — deliberately not a correlated subquery, whose re-evaluation during deletion is unspecified. Tested with a single 365-day-old snapshot surviving.
  Orphan test enumerates child tables from the LIVE schema (sqlite_master JOIN pragma_table_info WHERE c.name='scan_id', asserting >=12) rather than a hardcoded list, so a future table cannot escape it.
  Measurement (40 snapshots x ~300 assets, spanning 45 days): scans 40->27, assets 12000->8100, observations 24000->16200, evidence 48000->32400 (-32.5%), zero orphans. File did NOT shrink: 83.02 MB with 7223 freelist pages (~28 MB) and WAL grown to 19.39 MB. Growth is now BOUNDED (plateaus at ~30 days, reuses freed pages) but filesystem reclamation is still owed — Task 4.
Task 3 review: dispatched (base 8b12f02, head c85a66f)
Task 4: dispatched (base c85a66f) — space reclamation
Task 2 complete: e0b3c1f; review CLEAN. Guard placement verified total: validateSnapshot has exactly two production callers (SaveScan before any SQL; LatestSnapshot after rows load), LatestInventory inherits it, and there is exactly ONE non-test write to scans and ONE non-test read — both gated. 3/3 mutations killed, including one proving the load subtest is load-specific rather than incidental. Load test proven non-tautological: 'ok' is non-empty, valid UTF-8 and secret-free, so nothing but Valid() can reject it.
  LEDGER CORRECTION (the Task 2 entry above is wrong): the implementer's stated rationale for keeping BOTH checks does not hold. Valid() admits exactly four ASCII literals, so no value surviving it can be empty, invalid UTF-8, or secret-shaped — the UTF-8 and secret scans are unreachable on this field. Reviewer empirically deleted the required-string entry and the whole suite still passed. It is harmless redundancy (three lines of dead weight in a belt-and-braces validator), NOT the meaningful second gate the implementer claimed. Residual difference is error selection only: the map loop runs first, so a credential-shaped status returns ErrSensitiveSnapshot instead of the generic message; nothing matches on that sentinel for this field. Legitimate cleanup for a later task; keeping it blocks nothing.
  Compatibility verified: the load guard makes an out-of-vocabulary persisted status a hard read failure with no fallback, but every historical revision of overallStatus returned only complete|partial, so no shipped state.db can be bricked. Hard-fail matches the package's existing behaviour for corrupt scope_json.
  Informational, out of scope: internal/store/sqlite.go:247-290 interpolates the absolute database path into eight error messages. That is the store's own state.db path, never persisted snapshot content.
