# SDD ledger — plan: docs/superpowers/plans/2026-08-08-hook-severity-ladder.md

Design: docs/superpowers/specs/2026-08-08-hook-severity-ladder-design.md (12 grilled decisions)
Branch base: 59cbda0 (docs: plan hook severity ladder), working on master
Preflight: hook command plan complete (Tasks 1-5 + defect fix), all reviews clean
Known contract change: Task 1 removes the quiescent-rescan echo from scan --baseline --json delta (approved)
Task 1: dispatched (base 59cbda0)
Task 1 implementation: 11b821f; RED (cache flip produced drift), GREEN inventory/evidence/model + full suite + race; added model.MetadataCache, rewired internal/evidence/record.go to it; renamed acceptance cache-warm test to Quiescent, second rescan now asserts zero changes (other assertions strengthened not weakened)
Task 1 review: dispatched (base 59cbda0, head 11b821f)
Task 1 complete: commit 11b821f; review CLEAN. Reviewer confirmed exclusion is cache-key-only (blast radius: canonicalEvidence -> canonicalEvidenceByID -> Diff, nothing else), acceptance assertion strengthened not weakened, RED reproduced, 2/2 mutants killed (strip-all-metadata, strip-digest), clean-clone full suite + scripts gate PASS, 20x PASS. Empirical A/B on isolated homes: pre-fix run2=4 changes (2 real + 2 cache flips) run3=1 echo; fixed run2=2 real changes run3=0. Echo eliminated. Only observable contract change is delta.changes; cache still emitted on the record
Reviewer reminder for Task 5: stale sentences still present — hook-command-design.md:60 ("one-time cache-warm flood") and local-content-evidence-core-design.md:231 ("excludes only explicit observation timestamps")
Task 2: dispatched (base 11b821f)
Task 2 implementation: b1314fe; RED (undefined classify/rungRow/parseAssetID), GREEN package + 50x determinism + race; plan code verbatim, zero adaptations
Task 2 note for Task 3: hook.go already declares maxHookDetailRows = 20 — reuse, do not redeclare
Task 2 review: dispatched (base 11b821f, head b1314fe)
Task 2 review round 1: CHANGES REQUESTED — controller's plan design was wrong in two places.
  F1 HIGH: project assets are IDed project:sha256:<64hex>, so parseAssetID yields Name=<64hex> Host=sha256 → hook would print a digest as the asset name (regression: shipped hook resolved names via inventory Asset.Name; also collides with the no-digest privacy assertion). Editing package.json in a scanned root triggers it.
  F2 MEDIUM: mcp IDs carry NO version and MCP names may contain '@'; unconditional last-'@' split fabricates versions. Renaming ctx@dev -> ctx@prod pairs two unrelated servers into UPGRADED dev -> prod, erasing a real NEW + REMOVED and asserting a transition the tool never established (same unearned-claim class the design forbids for DOWNGRADED).
  F3 LOW: LastIndex '>0' guard untested; mutation to '>=0' survives the whole suite.
  Determinism cleared: sort keys form a total order, From/To cannot differ on a tie (200x + 500-iteration probe).
Task 2 fix round 1: ebbd3b1; RED all three reviewer defects + controller's added removed-asset case, GREEN report/cli/cmd/acceptance + race + 50x, 3 mutation kills verified.
  Fix shape: assetIdentity (pairing key) still ID-derived; new displayFor() takes Type/Name/Host from inventory.Assets when present, ID-parse fallback only for removed assets; added/removed maps carry assetChange{id,version} so a pair resolves display from the correct side.
  Version-split now restricted to the verified closed set {agent-plugin, agent-skill, ide-extension, pkg}; mcp/project/project-config/tool never split. Implementer independently grepped every collector ID construction to confirm, and corroborated: invalidAgentIdentity rejects '@' (agent split unambiguous) while invalidServerName permits '@' (mcp collision real).
  Implementer improved on the review: reviewer's guard test used mcp:claude-code:@internal, which after the fix no longer version-splits and so would not exercise the guard at all — changed to ide-extension:vscode:@internal, which does. Also found a PRE-EXISTING test row encoding the bug (mcp:...:@scope/server@1.0.0 asserting version 1.0.0, an unproducible ID) and replaced it.
  Removed digest-anchored assets render Name="(unnamed)", Host="" — guard is on digest SHAPE (^[0-9a-f]{64}$), not a prefix allowlist, so it also covers tool-executable and any future digest-anchored kind.
Task 2 re-review: dispatched (head ebbd3b1)
Task 2 re-review round 1: CHANGES REQUESTED — F1/F2/F3 all CLOSED and confirmed on a real binary in an isolated home with a real $HOME/Projects/demo/package.json (prints "CHANGED project project", no digest on any path; property-style table over every producible ID shape x {added,removed,changed} x {present,absent} found zero 64-hex in any row field). Version-carrying set independently re-grepped and confirmed complete; every producible versioned ID contains exactly one literal '@' (agents reject '@', ide normalizeIdentity rejects '@', escapePURLSegment percent-encodes it). UPGRADED can no longer be fabricated. All 5 mutations killed.
  NEW F1 MEDIUM (introduced by the fix): displayFor returns string(asset.Type) for present assets but the ID PREFIX for absent ones — and removed assets are absent by definition. So REMOVED rows print a different vocabulary than every other rung: mcp vs mcp-server, pkg vs package, project-config vs project, tool-executable vs tool. Also project-config is 14 chars and overflows the %-13s column, breaking the scannable-column contract. Contradicts the plan's own expected row (Type "mcp-server" on a REMOVED).
  WHY THE SUITE MISSED IT: fixtures use mcp-server:claude-code:github / :cursor:stale — a prefix NO collector emits (real is mcp:). rung_test.go:44 asserts a REMOVED row's Type only because the fictional ID's prefix coincidentally equals the model type. Tests were passing by coincidence.
  NEW F2 LOW: replacement fixtures are themselves unproducible (pkg:npm/@scope/... — escapePURLName percent-encodes to %40; ide-extension:vscode:@internal — normalizeIdentity rejects '@' in publisher), and rung.go:62's comment still justifies the last-'@' split by "npm scopes may contain @", which is false for all four versioned prefixes after the closed-set restriction.
Task 2 fix round 2: 7cdf8eb; prefix->AssetType mapping verified at all nine minting sites (complete set; no other model.Asset{ID:} construction outside tests), unknown prefixes fall through to the prefix. RED: 5 failures, incl. THREE assertions that had been passing by coincidence and became load-bearing once the fictional mcp-server: fixtures were replaced with real mcp: IDs. Column contract now locked by TestRungTypesFitTheScannableColumn (longest mapped type is ide-extension at 13). Round-1 guarantees re-verified: no 64-hex on any rendered line, no fabricated UPGRADED for ctx@prod/ctx@dev, project assets still distinct (assetIdentity untouched — the mapping affects display only). Defect B closed: pkg fixture corrected to the real percent-encoded form, parseAssetID comment now states the true invariant (every versioned prefix escapes '@' out of the name, so a versioned ID holds exactly one literal '@'), guard fixture labeled synthetic with its reason.
Carryover from the hook-command plan: cmd/ssc-init/main.go scanConfiguration failure branch untested (mutating it to exit 2 leaves the suite green) — dispatched as a test-only fix on main_test.go
CONSOLIDATED REVIEW (59cbda0..ca15607): CHANGES REQUESTED. Suite/vet/mod-verify/scripts/diff all green — every finding below is invisible to the tests and was found by adversarial probing + 16 live runs. 8/8 mutations killed: the suite is strong against what it MODELS.
  F1 HIGH (guarantee 3): fabricated UPGRADED with a dangling arrow when only ONE id encodes a version. agents/collector.go appends @version only when known, so a plugin gaining/losing its version field pairs into "UPGRADED demo (claude)   -> 1.2.0". Dates from b1314fe; NOT introduced by a later fix.
  F2 HIGH (guarantee 8): NON-DETERMINISM — unstable sort.Slice keyed only on (Rung,Type,Name,Host); rows colliding on all four but differing in From/To render in random order. 4000-iteration probe: 3068 vs 932 split across two byte streams. Reachable with real IDs (two publishers sharing an extension name; npm vs pypi same package name, Source cleared for packages). Violates the release-blocking CLAUDE.md determinism invariant.
  F3 MEDIUM (guarantee 4 half-applied): 7cdf8eb fixed the TYPE vocabulary but not the NAME. REMOVED prints the ID segment, every other rung prints Asset.Name — they disagree for ide-extension (usernamehw.errorlens vs errorlens) and pkg (npm/%40scope/server vs @scope/server).
  F4 MEDIUM (guarantee 9): isInitialBaseline fires on the SECOND run when the first baseline recorded zero assets, swallowing the first genuine NEW on a clean machine. Reviewer flags this as a CONTRACT question, not a patch.
  F5 MEDIUM (spec): best[identity] = row unconditionally, so UPGRADED overwrites UNVERIFIED — contradicts "highest rung wins" (UNVERIFIED ranks above UPGRADED). Live: a plugin upgraded while its tree is symlink-partial reports UPGRADED, hiding that the new bytes are unverifiable.
  F6/F7 LOW: one mcp-server: fiction survives in pretty_test.go:228; an 8-hex "digest" in rung_test.go:82; and every NEW/UPGRADED fixture places the added asset OUTSIDE the inventory, so tests exercise the parse fallback rather than the production branch — which is exactly why F3 was invisible.
  F8/F9/F10 INFO: pretty.go ISSUES could print a digest-anchored AssetID (unreachable in practice); the design doc claims an orphan test that does not exist in range; "(unnamed)" is undocumented.
  Seam hunt: all four suspicions cleared — the three classifier fix rounds are additive and mutually consistent; no later commit undid an earlier guarantee.
Fix round: F1/F2/F5 + F3 doc + F6/F7 fixtures dispatched. F4 escalated to the user as a contract decision.
Consolidated fix: d909b2b. F1 fixed by requiring BOTH endpoints to carry a version (else NEW + REMOVED — the tool never establishes a transition it did not see). F2 fixed with an unexported rungRow.key (asset ID) as final sort tiebreaker; stable sort explicitly rejected since it inherits map order. F5 fixed via note() for UPGRADED. F3 documented, not normalised (last-dot stripping breaks on names containing dots = another unearned claim). F6/F7 fixtures corrected.
  SIXTH DEFECT found by the implementer, unnamed by the review: a removed pkg:npm/six@1.0.0 overwrote the CHANGED row of a CURRENT pkg:npm/six@2.0.0 because both wrote best[identity]. A removed asset is absent from the current inventory, so no current record can belong to it — they are distinct assets and now get distinct rows.
  TRAP in the controller's prescribed F5 fix, caught empirically: note() ALONE would have silently killed UPGRADED in production. An upgrade mints a new asset ID, so all its records arrive as ADDED with complete evidence, which notes CHANGED(1) and beats UPGRADED(3) — verified with a variant binary, produced "CHANGED agent-plugin clean (claude)", a false claim since CHANGED means same-version-different-bytes. Rule added: an added asset's own new records are subsumed by the asset-level rung unless not complete (which still raises UNVERIFIED). Both directions under test.
F4 decision (user): plumb a real first-run signal from the scan service, not a heuristic. Dispatched.
F4 fix: 3dde840; BaselineScanner widened to return firstRun (!exists from the LatestSnapshot the service already makes); isInitialBaseline DELETED, not tuned. Rejected the smaller route (reading LatestSnapshot in the cli hook path) because it re-deserializes a snapshot for one bool, adds a second source of truth, and silently regresses to always-ladder wherever StatusReader is unwired — which is cmd's hook case today. Live: empty home silent -> first skill NEW (was "initial baseline recorded") -> second skill NEW -> silent; populated first run still prints the baseline line.
Ledger files committed (.superpowers/ is NOT gitignored and the prior plan's ledgers are tracked) — this was what tripped the scripts clean-tree gate; gate green again.
Truthfulness items: 3fad3aa. F8 guarded with the EXISTING digestSegment/parseAssetID helpers so pretty's ISSUES column renders the same (unnamed) placeholder the ladder uses — no second regexp, two surfaces agree. F9 closed by choice (a): wrote the claimed end-to-end test (runs the official fixture home with probes off AND on, since tool-executable only exists under probes; resolves evidence the way the renderer does; fails vacuity first), and the doc now names it and calls renderer tolerance a third defensive layer rather than the primary control. F10 documented incl. the multiplicity limit. Implementer corrected the controller's brief: manifests/lockfiles are project EVIDENCE, not project-config assets — only the four MCP project configs are.
FINAL REVIEW (ca15607..3fad3aa): CHANGES REQUESTED. 14 mutations, 0 survivors — the suite is tight on every STATED guarantee — but a seventh defect was found that no test or fixture ever modelled.
  F1 HIGH: added/removed are keyed by assetIdentity{type,host,name} in a MAP, but every family that appends @version can have TWO live assets under one identity. Second overwrites first -> a deleted surface produces NO row and the header undercounts. Real shape, no synthesis: ~/.vscode/extensions keeps one directory per installed version. Live: two errorlens versions deleted -> ONE "REMOVED ide-extension usernamehw.errorlens" row while the JSON delta carries both removals. Pairing arm: 2 removals + 1 addition -> one UPGRADED whose From endpoint is chosen by string sort, not by what was replaced. Violates the design's "one line per asset", "last is not invisible", README's "one line per changed asset", and CLAUDE.md's "Missing or failed coverage stays visible instead of being treated as success".
  Reviewer's explicit trap for the fixer: added/removed must STAY keyed by identity for the PAIRING decision, but the ROW SET must be per asset ID; note() is keyed by identity too, so collapsing there is the same bug one layer up.
  F2 LOW: HOST asymmetry undocumented — tool:go:cosign renders no host when present (packages clear Source) but "(go)" when removed; project-config carries Source "project-config" which renders in the host slot though it is a collector tag, not a host.
  F3 INFO: rung.go raises UNVERIFIED for unsupported evidence, while hook.go:60 and pretty.go:210 both explicitly EXCLUDE unsupported as "a deliberate non-claim". Masked today because an added package asset wins the row with NEW.
  F4 INFO: two ladder fixtures use ide-extension:vscode:big@1.0.0 / bravo@2.0.0; manifest.go always mints publisher.name, so a bare segment is unreachable — and F1 sat under exactly that fixture.
Fix round dispatched for F1/F2/F3/F4.
REMAINING AFTER F4: (a) fresh review of d909b2b + F4 (merge rules changed materially; interface widened); (b) three truthfulness items — design doc claims an orphan test that does not exist, "(unnamed)" undocumented, pretty.go ISSUES has an unreached digest-anchored AssetID path; (c) full verification + release build; (d) reinstall ~/.local/bin/ssc-init and move the tag off 994b43c.
CONTROLLER DECISION: after Task 2 fix round 2 lands, STOP per-task re-reviews and run ONE consolidated review of 59cbda0..HEAD. Rationale: four review rounds have each found real defects and two of those were introduced BY a previous fix, so the classifier has churned; a fresh reader on the final state is likelier to catch a seam between fixes than another narrow round. Tasks needing coverage in that consolidated pass: Task 6 (18872ff, never reviewed), Task 3 fix (0c81e62), Task 4 fix (34ffbe5), Task 2 fix round 2, and the main_test carryover.
Then: full verification (clean cache, race, vet, mod verify, scripts gate, release build) -> reinstall ~/.local/bin/ssc-init (still pre-ladder, so the wired SessionStart hook is not running what we built) -> tag decision (v0.1.0 at 994b43c predates BOTH the hook command and the ladder; everything from 57cfe78 on is untagged)
Task 3: dispatched (base b1314fe)
Task 3 implementation: 65f5177; RED (3 failures, old grouping output), GREEN report+race+50x+cmd; reused existing maxHookDetailRows (only adaptation); isolated-home 4-run demo: baseline line / silent / CHANGED after mutation / SILENT — echo fix confirmed end-to-end
PLAN GAP found by Task 3: internal/cli TestHookIsAdvisoryAcrossDriftCleanAndFailure (run_test.go:368) asserts old format; not in any task file list. Assigned to Task 5.
Task 3 review: dispatched (base b1314fe, head 65f5177)
Task 4: dispatched (base 65f5177)
Task 4 implementation: 08fb3c7; RED (bare DELTA added=1 counts), GREEN report+cli+race+50x; fixed two pretty goldens pinning old counts; demo NEW / CHANGED / (no changes)
Task 4 declined the shared render helper — correctly: brief said factor and use from BOTH but forbade touching hook.go. 8-line format block now duplicated hook.go:38-45 vs pretty.go. DEFERRED to Task 6 (after Task 3 review clears hook.go)
Task 3 review round 1: CHANGES REQUESTED — 1 LOW, test-only: the len(rows)==0 silence guard (hook.go:31-33) is unprotected; deleting it survives the suite, printing "0 changes since last snapshot" or a bare standing line when every change is unattributable. Reviewer supplied a RED-verified test. Everything else clean: cap favours high rungs (21 NEW + 5 REMOVED -> 20 NEW, 0 REMOVED), format matches spec byte-for-byte, dead code fully removed, 3/4 mutants killed. Known ceiling recorded (not a defect): isInitialBaseline false-positives only when the PREVIOUS snapshot had zero assets — accepted consequence of the pure-function design.
  Reviewer drove all five rungs on a real binary: NEW / CHANGED / UPGRADED (1.0.0 -> 1.1.0) / REMOVED, plus baseline -> silent -> CHANGED -> silent -> silent.
Task 3 fix round 1: 0c81e62 (test-only, hook_test.go); RED verified against BOTH mutations (guard deleted -> "0 changes since last snapshot" + standing line; standing line hoisted -> bare standing line). Implementer correction to the review: mutation B was ALSO already killed by TestWriteHookSummaryRendersLadder's anchored header regex; only the guard-deletion case was genuinely unprotected. New test covers the zero-drift path where nothing fired.
Task 4 review: dispatched (base 65f5177, head 08fb3c7) — CORRECTION: earlier ledger line claimed this was dispatched; it was not. Dispatched now.
Task 4 review round 1: CHANGES REQUESTED — 1 MEDIUM, test-only: the "scan --pretty is NOT capped" contract clause has zero coverage. Inserting the hook's 20-row cap into deltaSummary leaves the whole repo green. Reviewer predicted the exact failure path: the planned dedupe of the shared 8-line row block would naturally drag the hook's cap + overflow line into pretty, silently truncating an interactive inventory diff with no indication. THEREFORE: the no-cap test MUST land before the dedupe. Reviewer supplied a RED-verified test.
  Everything else clean: goldens strictly stronger than the counts they replaced (they now pin rung classification, which counts never touched); 30-row/UPGRADED/hostless/zero-classified adversarial cases pass; hook-vs-pretty rule inversion not crossed; mutations A (silent empty delta) and C (bare counts) killed.
  Duplication assessed behaviourally, not textually: a scratch test rendered the same (inventory, delta) through WriteHookSummary and WritePretty and compared the indented rows byte-for-byte — identical. The two copies have NOT diverged yet.
REPO GOTCHA discovered: running `go test ./...` inside a `git archive` export corrupts the export — scripts/build-darwin_test.go checks a NEWER tree out over the working directory (adds .git/, dist/, overwrites README and test files). Pinned-commit verification must exclude ./scripts. Worth adding to CLAUDE.md.
Task 4 fix round 1: 34ffbe5 (test-only, pretty_test.go); RED verified (cap mutation -> "got 20 rows"), GREEN + 20x. Adaptation: reviewer's fixture used bare model.Asset{ID}, which displayFor (ebbd3b1) resolves to empty Type/Name/Source -> row would render blank and the test would be permanently red for the wrong reason. Assets populated with AssetMCP/name/source matching what parseAssetID would derive; the reviewer's regex kept byte-for-byte, both assertions verbatim.
Task 6 (dedupe shared row formatter): dispatched (base 34ffbe5) — now safe: only the formatting block moves; cap and overflow line must stay hook-side, and 34ffbe5 fails if they migrate
Task 6 implementation: 18872ff; rungRow.render() added, both call sites now printer.line("  " + row.render()). Characterization test written BEFORE the refactor and passing both before and after; it pins a hard-coded literal (actual bytes) as well as hook==pretty, so identical drift in both surfaces would still fail. Cap/overflow confirmed hook-only by grep AND by TestWritePrettyDeltaLadderIsNotCapped passing — the predicted leak did not happen.
Task 5: dispatched (base 08fb3c7) — owns acceptance lifecycle, cli TestHookIsAdvisory (plan gap), README, 2 stale spec sentences
Task 5 implementation: 4cfe82a; RED both red tests (acceptance initial-baseline, cli NEW row), GREEN + 20x each; acceptance rewritten to 4 phases incl. run-4 silence locking the echo fix; README rewritten with the no-verdict sentence; hook-command-design Output marked Superseded, Testing item 4 rewritten; evidence-core 6.1 now names the cache key. Clean-clone verification: scripts gate PASS at 4cfe82a (in-place gate trips only on concurrent agents' uncommitted internal/report files)
Task 5 review: dispatched (base 08fb3c7, head 4cfe82a)
Task 5 complete: commit 4cfe82a; review CLEAN, empty failure set on a pristine clone (scripts gate PASS). Vacuity probe confirmed run 4 is not trivially satisfiable: neutering the run-3 mutation to identical bytes fails at run 3, so run 3 hard-gates run 4. Reverting the Task 1 cache fix is caught one phase EARLIER at run 2 — the acceptance test locks the echo fix as designed. CLI drift assertion strengthened not weakened (loose Contains -> exact header + anchored regex, and it pins that NEW carries no version). Every README claim verified against committed code; release-blocking no-verdict sentence present and unhedged. 4 doc corrections confirmed; grep sweep found no surviving stale claim outside banner-scoped history.
FOLLOW-UP from Task 5 review (informational, now actionable): docs/testing/2026-08-06-use-case-validation.md:362-366 still asserts the cache-warm changed delta and "diffing excludes only observation timestamps" — behaviourally false as of 11b821f. It is a revision-pinned dated report, so the fix is a superseded banner, not a rewrite. Owned by final cleanup.
